# frozen_string_literal: true

FactoryBot.define do
  factory :workflow do
    #   steps { [create(:step)] }
  end

  # trait :with_multiple_steps do
  #   steps { create_list(:step, 3) }
  # end
end
