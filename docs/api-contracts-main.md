# API Contracts: Main

This document will detail the API endpoints and their contracts for the 'main' part of the project.

## Summary
API endpoints are defined in `config/routes.rb` and handled by controllers in `app/controllers/`. Business logic is delegated to a service layer in `app/services/`.

*(To be generated: Detailed API Endpoints, HTTP Methods, Paths, Request/Response Payloads)*
