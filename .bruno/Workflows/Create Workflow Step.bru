meta {
  name: Create Workflow Step
  type: http
  seq: 1
}

post {
  url: {{base}}/workflows/:workflow_id/steps
  body: json
  auth: inherit
}

params:path {
  workflow_id: 2
}

body:json {
  {
    "name": "Test Step",
    "config": {
      "liquid_templates": {
        "name": "Test Step",
        "url": "{{base_url}}/api/test",
        "method": "get"
      }
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
