meta {
  name: Create Workflow
  type: http
  seq: 2
}

post {
  url: {{base}}/workflows
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{name}}",
    "connection_id": "{{connection_id}}"
  }
}

vars:pre-request {
  name: test workflow
  connection_id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
