<!DOCTYPE html>
<html>
  <%= render "layouts/shared/head" %>
  <body>
    <header id="header" class="w-full">
        <div class="navbar bg-neutral min-h-4">
            <div class="navbar-start">
                <button class="btn btn-ghost btn-sm">ABify</button>
            </div>
            <%= render "layouts/shared/main_menu" %>
        </div>
    </header>

    <main id="main" class="w-full h-full">
      <%= render "layouts/shared/flash" %>
      <%= yield %>
    </main>
  </body>
</html>
