services:
  app:
    build: .
    ports:
      - "10000:10000"
    volumes:
      - .:/rails
      - gem_cache:/usr/local/bundle/gems
    environment:
      - RAILS_ENV=development

volumes:
  gem_cache:
